import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function TodayScreen() {
  return (
    <ScreenPlaceholder
      title="Today"
      description="What to train today, and the way into a session."
      issue="#32"
    />
  );
}
