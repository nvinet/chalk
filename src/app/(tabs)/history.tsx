import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function HistoryScreen() {
  return (
    <ScreenPlaceholder
      title="History"
      description="Every session logged, as a list and as a calendar."
      issue="#36"
    />
  );
}
