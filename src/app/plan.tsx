import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function PlanScreen() {
  return (
    <ScreenPlaceholder
      title="Plan"
      description="The weekly schedule and how closely it was followed."
      issue="#33"
    />
  );
}
